import {
  Container,
  Typography,
} from "@material-ui/core";

const TodoApp = () => {

  return (
    <>
    <Container maxWidth="xs">
      <Typography variant="h3">
        To-Do's
      </Typography>
      </Container>
    </>
  )
}

export default TodoApp;
